name: CyberWatchdog Automation

on:
  schedule:
    - cron: '0 0 * * *'  # Runs daily at midnight UTC
  workflow_dispatch:  # Allows manual triggering of the workflow

jobs:
  update-repositories:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v3

    - name: Set up dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y jq

    - name: Run CyberWatchdog Script
      env:
        PAT_TOKEN: ${{ secrets.PAT_TOKEN }}  # For GitHub API calls
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}  # For pushing to the repo
        GITHUB_REPOSITORY: ${{ github.repository }}  # Your repo name
      run: |
        chmod +x ./cyberwatchdog.sh
        ./cyberwatchdog.sh
